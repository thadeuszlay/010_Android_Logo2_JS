<!DOCTYPE html>
<head>
	<link href='http://fonts.googleapis.com/css?family=Orbitron' rel='stylesheet' type='text/css'>
		<style>
			.andy{				
				height: 175px;
				width: 216px;
				background-color:rgba(100, 200, 10, 1);
				border-radius: 0 0 20px 20px; 
				position: relative;	
				left: 100px;
				top: 110px; 		
				margin: 70px;
				display: inline-block;			
			}

			.andy::before{
				content: "";
				color: white;
				width: 50px;
				height: 150px;
   			background-color: rgba(100, 200, 10, 1);
   			position: absolute;
   			left: -60px;
				border-radius:50px;   			
   			box-shadow:     
   				285px 0 0 rgba(100, 200, 10, 1),
    				190px 110px 0 rgba(100, 200, 10, 1),
    				95px 110px 0 rgba(100, 200, 10, 1);
    		}
    		.andy::after{
				content: "\0000a0V";
				color: rgba(100, 200, 10, 1);
				font-size: 140px;	
				font-family: 'Orbitron', sans-serif;
				line-height: 60px;
				width: 216px;
				height: 108px;
   			background-color: rgba(100, 200, 10, 1);
   			position: absolute;
   			left: 0px;
   			top: -118px;
   			border-radius: 216px 216px 0 0; 
   			background: radial-gradient(circle, white 10%, transparent 0%), rgba(100, 200, 10, 1);
   			background-size: 50% 100%;
        	}
			        	
        	.andyB::before{
				content: "";
				color: white;
				width: 50px;
				height: 150px;
   			background-color: red;
   			position: absolute;
   			left: -60px;
				border-radius:50px;   			
   			box-shadow:     
   				285px 0 0 rgba(100, 200, 10, 1),
    				190px 110px 0 rgba(100, 200, 10, 1),
    				95px 110px 0 rgba(100, 200, 10, 1);
    		}
    		
        	@keyframes move{
				10% {
					top: 	100px;
					left: 90px;
				}
				40%{
					top: 90px;	
					left: 70px;
				}
				70%{
					top: 120px;
					left: 110px;
				}
				90%{
					top: 100px;
					left: 120px;
				}
			}
        	@-moz-keyframes move{
				10% {
					top: 	100px;
					left: 90px;
				}
				40%{
					top: 90px;	
					left: 70px;
				}
				70%{
					top: 120px;
					left: 110px;
				}
				90%{
					top: 100px;
					left: 120px;
				}
			}
        	@-webkit-keyframes move{
				10% {
					top: 	100px;
					left: 90px;
				}
				40%{
					top: 90px;	
					left: 70px;
				}
				70%{
					top: 120px;
					left: 110px;
				}
				90%{
					top: 100px;
					left: 120px;
				}
			}
			.action{
			animation: move 2s infinite;
			}		
		</style>
		</head>
		<div class="andy" id="domo"></div>	
<script type="text/javascript">
function toggleClass(element, className){
    if (!element || !className){
        return;
    }
    
    var classString = element.className, nameIndex = classString.indexOf(className);
    if (nameIndex == -1) {
        classString += ' ' + className;
    }
    else {
        classString = classString.substr(0, nameIndex) + classString.substr(nameIndex+className.length);
    }
    element.className = classString;
}
document.getElementById("domo").addEventListener("click", function(){
    toggleClass(document.getElementById("domo"), ' action');    
});
document.getElementsByTagName('body')[0].onkeydown = function(e) { 
   var ev = e || window.event;
    if (ev.keyCode===83){
		// Create a new style tag
		var style = document.createElement("style");

		// Append the style tag to head
		document.head.appendChild(style);
		
		// Grab the stylesheet object
		sheet = style.sheet

		// Use addRule or insertRule to inject styles
		if("insertRule" in sheet) {
			sheet.insertRule('.andy:before { background-color: rgba(100, 200, 10, 1); }', 0);
			
			sheet.insertRule('.andy:before {box-shadow: 285px 0 0 rgba(100, 200, 10, 1), 190px 110px 0 rgba(100, 200, 10, 1), 95px 110px 0 rgba(100, 200, 10, 1);;}', 0);
			sheet.insertRule('.andy:after {background-color: rgba(100, 200, 10, 1);}', 0);
		}
    
    
        document.getElementById("domo").style.backgroundColor = "red";
        
        
    }
    
    else if(ev.keyCode===65){
		// Create a new style tag
		var style = document.createElement("style");

		// Append the style tag to head
		document.head.appendChild(style);
		
		// Grab the stylesheet object
		sheet = style.sheet

		// Use addRule or insertRule to inject styles
		if("insertRule" in sheet) {
			sheet.insertRule('.andy:before { top: -110px; }', 0);
			
			document.getElementById("domo").style.backgroundColor = "rgba(100, 200, 10, 1)";
			sheet.insertRule('.andy:before {box-shadow: 285px 0 0 rgba(100, 200, 10, 1), 190px 110px 0 rgba(100, 200, 10, 1), 95px 110px 0 rgba(100, 200, 10, 1);;}', 0);
			sheet.insertRule('.andy:after {background-color: rgba(100, 200, 10, 1);}', 0);
		}
		else if("addRule" in sheet) {
			sheet.addRule('.andy:before','background-color: pruple;');
		}
	 }
	 else if(ev.keyCode===87){
		var style = document.createElement("style");

		// Append the style tag to head
		document.head.appendChild(style);
		
		// Grab the stylesheet object
		sheet = style.sheet

		// Use addRule or insertRule to inject styles
		if("insertRule" in sheet) {
			sheet.insertRule('.andy:before { background-color: rgba(100, 200, 10, 1); }', 0);			
			document.getElementById("domo").style.backgroundColor = "rgba(100, 200, 10, 1)";
			sheet.insertRule('.andy:before {box-shadow: 285px 0 0 rgba(100, 200, 10, 1), 190px 110px 0 rgba(100, 200, 10, 1), 95px 110px 0 rgba(100, 200, 10, 1);}', 0);
			
			sheet.insertRule('.andy:after {background-size: 50% 150%;}', 0);		
		}
		else if("addRule" in sheet) {
			sheet.addRule('.andy:after','background-color: yellow;');
		}
	 }
	 else if(ev.keyCode===68){
		var style = document.createElement("style");

		// Append the style tag to head
		document.head.appendChild(style);
		
		// Grab the stylesheet object
		sheet = style.sheet

		// Use addRule or insertRule to inject styles
		if("insertRule" in sheet) {
			sheet.insertRule('.andy:before { background-color: rgba(100, 200, 10, 1); }', 0);			
			document.getElementById("domo").style.backgroundColor = "rgba(100, 200, 10, 1)";
			sheet.insertRule('.andy:after {background-color: rgba(100, 200, 10, 1);}', 0);

			sheet.insertRule('.andy:before {box-shadow: 285px -110px 0 rgba(100, 200, 10, 1), 190px 110px 0 rgba(100, 200, 10, 1), 95px 110px 0 rgba(100, 200, 10, 1);;}', 0);
		}
		else if("addRule" in sheet) {
			sheet.addRule('.andy:before','box-shadow: 285px 0 0 blue, 190px 110px 0 rgba(100, 200, 10, 1), 95px 110px 0 rgba(100, 200, 10, 1);');
		}
	 }
 	 else if(ev.keyCode===88){
		var style = document.createElement("style");

		// Append the style tag to head
		document.head.appendChild(style);
		
		// Grab the stylesheet object
		sheet = style.sheet

		// Use addRule or insertRule to inject styles
		if("insertRule" in sheet) {
			sheet.insertRule('.andy:before { background-color: rgba(100, 200, 10, 1); }', 0);			
			document.getElementById("domo").style.backgroundColor = "rgba(100, 200, 10, 1)";
			sheet.insertRule('.andy:after {background-color: rgba(100, 200, 10, 1);}', 0);

			sheet.insertRule('.andy:before {box-shadow: 285px 0 0 rgba(100, 200, 10, 1), 190px 130px 0 rgba(100, 200, 10, 1), 95px 110px 0 rgba(100, 200, 10, 1);}', 0);
		}
		else if("addRule" in sheet) {
			sheet.addRule('.andy:before','box-shadow: 285px 0 0 rgba(100, 200, 10, 1), 190px 130px 0 rgba(100, 200, 10, 1), 95px 110px 0 rgba(100, 200, 10, 1);');
		}
	 }	 
 	 else if(ev.keyCode===89){
		var style = document.createElement("style");

		// Append the style tag to head
		document.head.appendChild(style);
		
		// Grab the stylesheet object
		sheet = style.sheet

		// Use addRule or insertRule to inject styles
		if("insertRule" in sheet) {
			sheet.insertRule('.andy:before { background-color: rgba(100, 200, 10, 1); }', 0);			
			document.getElementById("domo").style.backgroundColor = "rgba(100, 200, 10, 1)";
			sheet.insertRule('.andy:after {background-color: rgba(100, 200, 10, 1);}', 0);

			sheet.insertRule('.andy:before {box-shadow: 285px 0 0 rgba(100, 200, 10, 1), 190px 110px 0 rgba(100, 200, 10, 1), 95px 130px 0 rgba(100, 200, 10, 1);}', 0);
		}
		else if("addRule" in sheet) {
			sheet.addRule('.andy:before','box-shadow: 285px 0 0 rgba(100, 200, 10, 1), 190px 110px 0 rgba(100, 200, 10, 1), 95px 120px 0 rgba(100, 200, 10, 1);');
		}
	 }	 
}
</script>